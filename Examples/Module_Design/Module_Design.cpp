/* Pattern 1 */

// 显示多项式poly
Status DisplayPoly1(Polynomial poly)
{
    // 多项式不存在
    if(!poly) return ERROR;

    // 多项式存在，显示多项式
    //....
}


/* Pattern 2 */ 

// 显示多项式poly
Status DisplayPoly2(Polynomial poly)
{
    // 多项式不存在
    if(!poly){
        printf("多项式不存在！\n")
        return ERROR;
    }

    // 多项式存在，显示多项式
    //....
}
